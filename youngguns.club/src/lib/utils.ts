// import { stripUnit } from 'polished'

// export const fluid = ({
//   min,
//   max,
//   minViewWidth = '320px',
//   maxViewWidth = '2048px'
// }) => `
//   calc(${min} + ${stripUnit(max) -
//   stripUnit(min)} * (100vw - ${minViewWidth}) / ${stripUnit(maxViewWidth) -
//   stripUnit(minViewWidth)});
//   @media screen and (min-width: ${maxViewWidth}) {
//     font-size: ${max};
//   }
// `

export {}
